# ---------------------------------------------------------------------------
# Airflow environment variables
# Copy to .env and customise before running docker-compose up
# ---------------------------------------------------------------------------

# Airflow core
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__LOAD_EXAMPLES=False
AIRFLOW__CORE__FERNET_KEY=ZmDfcTF7_60GrrY167zsiPd67pEvs0aGOv2oasOM1Pg=
AIRFLOW__WEBSERVER__SECRET_KEY=supersecretkey

# Postgres (used by Airflow metadata DB)
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow

# AWS – update these to match your terraform outputs / environment
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=bigdata-project-data-lake
KAGGLE_DATASET_REF=bulter22/airline-data
DATASET_SUBDIR=airline_data

# If running on EC2 with an IAM instance profile, leave these empty.
# If running locally, set your credentials here:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Kaggle API credentials for the raw landing step in `project_dag_ingest`
# and the standalone `scripts/load_kaggle_raw_to_s3.py` helper.
# Prefer injecting these securely in your deployment environment.
# KAGGLE_USERNAME=
# KAGGLE_KEY=
# Optional: set this to a larger disk path if the dataset is too large for /tmp.
# KAGGLE_DOWNLOAD_DIR=

